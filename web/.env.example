# Absolutny URL API (serwer Fastify na LAN). Puste = ten sam origin (dev używa proxy Vite).
# Na Cloudflare Pages ustaw jako zmienną środowiskową build-time, np.:
#   VITE_API_BASE=https://mag-api.twojafirma.pl
VITE_API_BASE=
