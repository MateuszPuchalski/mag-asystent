{
  "name": "@wertis/server",
  "private": true,
  "version": "0.41.0",
  "type": "module",
  "description": "WERTIS — serwer API + worker Sfery (Fastify, SQLite).",
  "main": "dist/index.js",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "worker": "tsx watch src/worker/worker.ts",
    "seed": "tsx src/db/seed.ts",
    "seed:scenariusze": "tsx src/db/seed-scenariusze.ts",
    "build": "tsc -p tsconfig.build.json && node -e \"const f=require('fs');f.copyFileSync('src/db/schema.sql','dist/db/schema.sql');f.mkdirSync('dist/web',{recursive:true});f.copyFileSync('src/web/biuro.html','dist/web/biuro.html')\"",
    "start": "node dist/index.js",
    "start:worker": "node dist/worker/worker.js",
    "test": "tsx --test src/*.test.ts src/**/*.test.ts",
    "reconcile": "tsx src/reconcile-run.ts",
    "reslot": "tsx src/reslot-run.ts"
  },
  "dependencies": {
    "fastify": "^5.2.1",
    "mssql": "^11.0.1"
  },
  "devDependencies": {
    "@types/mssql": "^9.1.7",
    "@types/node": "^22.13.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
