# WERTIS — ustawienia środowiska (szablon)
#
#   cp wertis.env.example wertis.env     # i uzupełnij wartości
#   npm start                            # API
#   npm -w server run start:worker       # worker, drugie okno
#
# JEDEN plik dla OBU procesów — czytają go z dysku same, nie trzeba go nigdzie
# przepisywać ani wołać `source`. API i worker to osobne procesy: gdyby tylko
# jeden dostał SGT_MODE=mssql, zapisy po cichu wylądowałyby w lokalnej bazie
# aplikacji zamiast w Subiekcie, a zadanie i tak zgłosiłoby sukces.
#
# Szukany obok pliku wykonywalnego, a przy uruchomieniu z repo — w katalogu
# roboczym i katalogach nad nim: npm startuje skrypty w server/, a plik leży
# piętro wyżej. Inną ścieżkę wskazuje WERTIS_ENV_FILE.
#
# Zmienne środowiskowe MAJĄ PIERWSZEŃSTWO nad tym plikiem (wygodne w dev).
# Bezpieczeństwo nie stoi na tej regule, tylko na /api/health, które porównuje
# tryb obu procesów i krzyczy przy rozjeździe.
#
# wertis.env jest w .gitignore — trzyma hasło.
#
# ── Jak czytać ten plik (0.489.0) ───────────────────────────────────────────
# Nie trzeba go czytać w całości. Klucze są trzech rodzajów:
#
#   1. USTAWIA INSTALATOR — połączenie z Subiektem, magazyny, pole
#      lokalizacji, zdjęcia kartotek, port. Ręczna zmiana zwykle jest
#      pomyłką; do zmiany służy instalator z -TylkoKonfiguracja.
#   2. DECYZJE WŁAŚCICIELA — Allegro, progi dat, terminy zwrotów, Copilot,
#      kopie na innym dysku. Zmienia się je rzadko i świadomie.
#   3. ZAAWANSOWANE — pokrętła z dobrą wartością domyślną. Zostaw.
#
# Który klucz jest którego rodzaju, co ustawia i skąd serwer wziął obecną
# wartość, pokazuje panel: Ustawienia → Konfiguracja serwera (administrator).
# Klucz, którego nie czyta żaden program, zgłasza /api/health jako literówkę.
#
# Stałe ze struktury bazy (kody DOK_TYP_*, wyrażenie bufora) i pokrętła
# pamięci podręcznej zdjęć przestały być kluczami w 0.489.0.

# ── Tryb ────────────────────────────────────────────────────────────────────
# seeded = dane demo z magmat.xlsx, ZERO kontaktu z Subiektem (pilot, Etap 0);
#          na pustej bazie loguje się od razu kontem demo admin/admin (0.53.2)
# mssql  = prawdziwa baza Subiekta GT (Etap 1)
export SGT_MODE=mssql

# ── Połączenie z MSSQL Subiekta ─────────────────────────────────────────────
export MSSQL_SERVER=localhost
export MSSQL_INSTANCE=INSERTGT      # albo zamiast tego: export MSSQL_PORT=1433
export MSSQL_DATABASE=NAZWA_BAZY    # nazwa podmiotu (pasek tytułu Subiekta)
export MSSQL_USER=wertis            # login z docs/subiekt-gt-edu-setup.md §2
export MSSQL_PASSWORD=silne-haslo

# ── Kolumna lokalizacji ─────────────────────────────────────────────────────
# Wersje z KSeF nie mają natywnego tw_Lokalizacja — lokalizacja siedzi
# w jednym z ośmiu pól własnych tw_Pole1..8 (każde varchar(50)).
export MSSQL_LOC_COLUMN=tw_Pole1

# ── Magazyny ([WERYFIKUJ] — SELECT z DEPLOY §6) ─────────────────────────────
# Magazyn skutku rozstrzyga, którym trybem idzie dokument.
export MAG_ID_MAG=1
export MAG_ID_MGP=2
export MAG_ID_ZWROTY=3

# Magazyn ODPADU — towar z oceny „utylizacja" (0.211.0). ZERO ZNACZY
# WYŁĄCZONE i taka jest domyślna wartość: pomyłka w numerze WYSTAWIA
# DOKUMENT przesuwający złom na cudzy magazyn, a MM się nie cofa jednym
# kliknięciem. Bez tego wpisu ocena „utylizacja" zapisuje się jak dotąd
# i nie zakłada koszyka.
#export MAG_ID_ODP=4

# Magazyn SERWISOWY — towar, którego zabrakło w dostawie (0.391.0). ZERO
# ZNACZY WYŁĄCZONE, z tego samego powodu co przy odpadzie wyżej. Po co ten
# ruch: fakturę zakupu Subiekt księguje w całości, więc brakujący towar wisi
# na stanie jako sprzedawalny i obiecuje się klientom. Bez tego wpisu
# zgłoszenie braku zapisuje się jak dotąd i nie rusza stanu.
#export MAG_ID_SERWIS=5

# ── Zamówienia u dostawcy ([WERYFIKUJ] — SELECT-y z DEPLOY §6) ──────────────
# Karta towaru pokazuje, co jest zamówione i jeszcze nie przyjechało.
# Bez tych dwóch wpisów działa, tylko mniej dokładnie — patrz niżej.
#
# Które dok_Status znaczą „zamówienie otwarte". Struktura InsERT mówi tylko
# „5..8 — zamówienia (różne stany realizacji)", więc domyślne bierze wszystkie
# cztery. To ZAŁOŻENIE: sprawdź rozkład u siebie i zawęź.
#export DOK_STATUS_ZD_OTWARTE=5,6,7,8
#
# Kolumna dok_Pozycja z ilością JUŻ ODEBRANĄ.
#
# UWAGA: w części wersji Subiekta TAKIEJ KOLUMNY NIE MA WCALE — sprawdzono to na
# bazie 1.8731.31.6933, gdzie dok_Pozycja nie niesie stopnia realizacji w żadnym
# polu. Wtedy poprawnym ustawieniem jest WARTOŚĆ PUSTA: /api/health przestaje
# zgłaszać problem, którego nie da się rozwiązać ustawieniem, a karta dalej
# opisuje ilość jako oszacowanie.
#
# Zostawiona nazwa nieistniejącej kolumny daje to samo zachowanie, ale plus
# ostrzeżenie, którego nie można spełnić — a takie ostrzeżenia uczą ignorowania
# ostrzeżeń. Nazwę sprawdź zapytaniem z DEPLOY.md §6.
#export MSSQL_ZD_ZREAL_COLUMN=
#
# Termin realizacji zamówienia. Puste = karta pisze „termin nieznany", co jest
# uczciwsze niż podstawienie daty wystawienia w miejsce obietnicy dostawy.
#export MSSQL_ZD_TERMIN_COLUMN=
#
# Kolumna wskazująca dokument KORYGOWANY (0.201.0). Po niej automat znajduje
# korektę do zwrotu i zamyka go bez przepisywania numeru ręką. Nazwa sprawdzona
# na bazie firmy; trójka `dok_DoDokId` / `dok_DoDokNrPelny` / `dok_DoDokDataWyst`
# odpowiada właściwościom obiektu Sfery.
#
# Puste = automat wyłączony, numery wpisuje biuro. Tak samo działa brak kolumny
# albo brak prawa do niej: import sprzedaży schodzi szczebel niżej i mówi o tym
# w /api/health, zamiast się wywrócić.
#export MSSQL_KOREKTA_COLUMN=dok_DoDokId
#
# Kody dokumentów, które ODDAJĄ TOWAR NA STAN po zwrocie: 6 = KFS (korekta
# faktury sprzedaży), 14 = ZW (zwrot detaliczny). Oba ze struktury bazy
# 1.8731.31.6933. Firma księgująca zwroty do paragonu inaczej przestawia to tu.
#export DOK_TYPY_KOREKT=6,14

# ── Lista rozkładania: co na niej ląduje ────────────────────────────────────
#
# Typy dokumentów uznawanych za DOSTAWĘ do rozłożenia. Domyślnie 1 — sama
# faktura zakupu. Tak jest w firmie, dla której to powstało: PZ pochodzi
# z zupełnie innego procesu i na liście pracy magazyniera byłoby szumem.
#
# Ustaw 1,10, jeśli u Was towar wchodzi OBIEMA drogami — fakturą zakupu i
# przyjęciem zewnętrznym. Dla magazyniera to wtedy ta sama praca: paleta
# do rozłożenia.
#export DOK_TYPY_DOSTAW=1
#
# Ile dni wstecz sięga lista dostaw. Domyślnie 14.
#
# To okno IMPORTU — dokument spoza niego nie trafia do read-modelu w ogóle,
# a od 0.12.3 rządzi też zakresem samej listy (wcześniej trasa miała 14 zaszyte
# i to ustawienie działało tylko w połowie). Dostawa NIEROZŁOŻONA DO KOŃCA
# zostaje widoczna niezależnie od wieku, więc skrócenie okna nie kasuje z ekranu
# niedokończonej pracy.
#export DOK_DNI_WSTECZ=14

# ── Worker Sfery: dokumenty MM (DEPLOY §6 etap 2, sfera-worker/README.md) ───
#
# Domyślnie WYŁĄCZONE: zadania MM kończą się czytelnym błędem, a dokument
# wystawia biuro w Subiekcie. Włączenie przenosi zadania MM do osobnej usługi
# wertis-sfera (C#, COM Sfery — wymaga licencji) i każe workerowi Node ich
# nie dotykać. Nie włączaj bez działającego exe: MM wisiałyby w kolejce,
# a serwer przy SGT_MODE=seeded w ogóle odmówi startu.
#export SFERA_WORKER=1
#
# Operator Subiekta z prawem wystawiania MM (konto Subiekta, NIE login SQL).
#export SFERA_OPERATOR=
#export SFERA_OPERATOR_HASLO=
#
# Login SQL dla Sfery — DRUGA para, obok operatora. Przy autentykacji mieszanej
# Sfera potrzebuje obu: operator to użytkownik Subiekta, ten login otwiera bazę
# (przykład producenta w docs/sfera-com.md §1).
#
# To NIE jest MSSQL_USER. Tamten login ma z założenia SELECT na sześciu tabelach
# i UPDATE na dwóch kolumnach; Sfera wystawia dokumenty i potrzebuje pełnych
# praw podmiotu. Pusty klucz = Sfera nie dostaje loginu SQL (autentykacja
# Windows).
#export SFERA_SQL_LOGIN=
#export SFERA_SQL_HASLO=
#
# Trzy wartości, których dokumentacja publiczna nie podaje LICZBOWO — stąd
# w pliku, a nie w kodzie: poprawka kosztuje wtedy restart usługi, a nie
# przebudowanie exe na innej maszynie. Domyślne działają, dopóki sonda nie
# powie inaczej (sfera-worker/sonda.ps1).
#export SFERA_PROGID=InsERT.GT
#export SFERA_PRODUKT=1          # gtaProduktSubiekt
#export SFERA_AUTENTYKACJA=0     # gtaAutentykacjaMieszana; Windows ma inną
#
# Tryb uruchomienia Subiekta: maska bitowa UruchomEnum. Domyślne 6 to
# gtaUruchomNowy (0x2) | gtaUruchomWTle (0x4), czyli WŁASNA instancja bez okna.
# Usługa nie ma pulpitu, a podłączanie się do Subiekta otwartego przez człowieka
# kończy się błędem 0x8004132B — zmierzone u klienta (docs/sfera-com.md §2f).
#export SFERA_TRYB_URUCHOMIENIA=6
#
# Automatyczny ZW do paragonu (0.349.0). Po zapisaniu kwoty zwrotu w panelu
# worker Sfery sam wystawia ZW z pełną wartością towaru. Potrącenie zostaje
# w zwrocie pieniędzy Allegro. Faktury (KFS) zostają ręczne.
# Wymaga SFERA_WORKER=1. Włączaj dopiero po pierwszym ZW przy właścicielu
# (DEPLOY, akapit 0.349.0).
#export SFERA_ZW=1
#
# Kategoria wydania na ZW (0.463.0). Okno Subiekta wypełnia ją samo, szkic
# z COM zostawia pustą. Numer jest w bazie biura, nie w kodzie — odczytaj go
# z ZW wystawionego ręcznie: wertis-sfera-worker.exe --zrzut <dok_Id ZW>,
# pole WydanieKatId. Puste = worker nie ustawia kategorii.
#export SFERA_ZW_WYDANIE_KAT_ID=
#
# tw_Id kartoteki „PRZESYŁKA" z paragonów Allegro. Przy SFERA_ZW=1 wymagane:
#   SELECT tw_Id FROM tw__Towar WHERE tw_Symbol = 'PRZESYŁKA'
#export TW_ID_PRZESYLKA=943

# ── Zdjęcia kartotek na karcie towaru (0.30.0) ──────────────────────────────
#
# Domyślnie WYŁĄCZONE (puste ZDJECIA_ZRODLO): karta pokazuje pusty slot i nikt
# nie pyta bazy Subiekta o nic. Ten jeden klucz jest zarazem wyłącznikiem
# i wyborem źródła — dwa osobne mogłyby się rozjechać ze sobą.
#
# USTALONE na bazie tej firmy (2026-08-08): zdjęcia leżą w tw_ZdjecieTw.
# Komplet z uzasadnieniem — docs/subiekt-gt-struktura.md, rozdział „Gdzie
# Subiekt trzyma zdjęcie kartoteki". Wymaga siódmego GRANT SELECT na tę tabelę.
#
#export ZDJECIA_ZRODLO=blob
#export ZDJECIA_TABELA=tw_ZdjecieTw
#export ZDJECIA_KOLUMNA_KLUCZA=zd_IdTowar
#export ZDJECIA_KOLUMNA=zd_Zdjecie
# Kartoteka może mieć KILKA zdjęć („Ustaw jako główną" i „Sortuj" w Subiekcie).
# zd_IdTowar jest kluczem OBCYM, więc porządku nie domyka — robi to dopiero
# zd_Id. Bez tego serwer nie wystartuje: wybór byłby losowy przy każdym odczycie.
#export ZDJECIA_KOLUMNA_GLOWNE=zd_Glowne
#export ZDJECIA_KOLUMNA_KOLEJNOSC=zd_Id
#
# Wariant „zdjęcia leżą w katalogu, nie w bazie". Konto usługi wertis-api musi
# mieć do niego dostęp do ODCZYTU.
#export ZDJECIA_ZRODLO=plik
#export ZDJECIA_KATALOG=D:\zdjecia
#export ZDJECIA_WZORZEC_PLIKU={symbol}.jpg
#
# Serwer NIE zmniejsza obrazów (zero modułów natywnych), więc jedyną obroną
# przed kartoteką ze skanem 20 MB jest odmowa. Ile naprawdę ważą zdjęcia
# w tej bazie, pokaże /api/health po pierwszym dniu pracy.
#export ZDJECIA_MAX_KB=2048

# ── Dodawanie zdjęcia kartoteki z kolektora (0.88.0) ────────────────────────
#
# Kartoteka bez zdjęcia pokazuje na karcie pusty slot. Ten klucz zamienia go
# w przycisk: magazynier robi zdjęcie albo wybiera je z galerii, serwer wycina
# tło, a człowiek ogląda wynik przed zapisem.
#
# JEDEN KLUCZ jest zarazem wyłącznikiem i wyborem miejsca zapisu — tak samo
# jak ZDJECIA_ZRODLO wyżej i z tego samego powodu.
#
#   puste    (domyślnie) zdjęcia nie da się dodać; karta wygląda jak przed 0.88.0
#   wertis   zdjęcie zostaje w bazie WERTIS i widać je na karcie — DZIAŁA ZAWSZE
#   subiekt  dodatkowo wchodzi do kartoteki w Subiekcie — MA WARUNKI WSTĘPNE
#
# UWAGA: `subiekt` NIE jest równorzędnym wyborem wobec `wertis`. Zakłada, że na
# tej instalacji działa już ODCZYT zdjęć — czyli ZDJECIA_ZRODLO=blob wraz
# z ZDJECIA_TABELA, ZDJECIA_KOLUMNA_KLUCZA i ZDJECIA_KOLUMNA_GLOWNE, przy
# SGT_MODE=mssql. Bez kompletu SERWER ODMAWIA STARTU: to nie jest „funkcja nie
# zadziała", tylko wertis-api nie wstaje (NSSM melduje SERVICE_PAUSED). Powód
# stoi w logu usługi, a wycofanie polega na wyczyszczeniu tego klucza.
#
# Gdy nie masz pewności, czy odczyt zdjęć jest włączony — zostaw `wertis`.
# Zdjęcia będą działać na kolektorze od razu, a do kartoteki przeniesiesz je
# później, bez zmiany czegokolwiek w kolektorze.
#
# `subiekt` żąda ÓSMEGO uprawnienia i jest to PIERWSZE prawo dopisywania
# wiersza do bazy firmy, jakie ta aplikacja ma:
#
#   GRANT INSERT ON dbo.tw_ZdjecieTw TO wertis;
#
# Nadaje je instalator uruchomiony z -ZdjeciaZapis. Bez tego prawa funkcja NIE
# przestaje działać: zdjęcie leży w bazie WERTIS i widać je na karcie, a zadanie
# `set_zdjecie` stoi w kolejce z czytelnym błędem — tak samo jak kod kreskowy
# działa przez `ean_alias` mimo nieudanego `set_ean`.
#
#export ZDJECIA_DODAWANIE=wertis
#
# Ile kilobajtów wolno przyjąć od kolektora. Kolektor zmniejsza zdjęcie przed
# wysyłką (1600 px), więc przekroczenie tej liczby znaczy, że coś poszło
# z jego pominięciem — i wtedy odmowa jest odpowiedzią właściwą.
#export ZDJECIA_UPLOAD_MAX_KB=512
#
# Ile minut żyje podgląd między zrobieniem zdjęcia a jego zatwierdzeniem.
# Człowiek ogląda wycięte tło kilkanaście sekund; kwadrans to zapas na
# przerwane połączenie i na kolektor odłożony w połowie roboty.
#export ZDJECIA_PODGLAD_MIN=15

# ── Usługa usuwania tła: wertis-tlo (0.88.0, tlo-worker/README.md) ──────────
#
# CZWARTY proces, obok wertis-api, wertis-worker i wertis-sfera. Model chodzi
# na runtime ONNX, czyli na module natywnym, a serwer WERTIS ma dwie zależności
# i zero modułów natywnych — ta sama reguła, przez którą zdjęcia kartotek nie są
# skalowane, a loga dostawców przerabia przeglądarka.
#
# Domyślnie WYŁĄCZONE (puste TLO_URL). Bez usługi zdjęcia zapisują się Z TŁEM
# i kolektor mówi o tym wprost — funkcja nie przestaje działać, tylko robi mniej.
#export TLO_URL=http://127.0.0.1:8791
#
# Ile milisekund czeka SERWER. To granica cierpliwości człowieka stojącego przy
# regale, nie granica modelu: po jej upływie podgląd pokazuje zdjęcie z tłem.
#export TLO_TIMEOUT_MS=20000
#
# Poniższe dwa klucze czyta WYŁĄCZNIE usługa wertis-tlo.
# Plik modelu; domyślnie model\u2netp.onnx obok exe.
#export TLO_MODEL=C:\wertis\tlo-worker\model\isnet-general-use.onnx
# Dłuższy bok zapisywanego zdjęcia.
#export TLO_BOK=1024

# ── Pozycje dokumentu, które NIE są towarem (0.35.0) ────────────────────────
#
# Na fakturze zakupu bywa wiersz PRZESYŁKA / „koszt transportu". Nie ma go czym
# zeskanować ani gdzie położyć, więc wypada z rozkładania W OGÓLE — nie da się
# go odłożyć, pominąć ani zgłosić jako problem, i nie wisi do końca dnia jako
# jedyna niezałatwiona pozycja. Uzasadnienie: server/src/pomijane.ts.
#
# Wpisuje się SYMBOL kartoteki (CSV), nie opis — opis jest polem swobodnym
# i różni się między dostawcami. Wielkość liter, polskie ogonki i myślniki nie
# mają znaczenia, więc „przesylka" i „PRZESYŁKA" to jedno ustawienie.
# Puste = funkcja wyłączona, wszystkie pozycje idą na listę jak dotąd.
#export POZYCJE_NIE_TOWAROWE=PRZESYŁKA

# ── Konto Allegro (0.53.0, po 0.140.0 samo połączenie) ──────────────────────
#
# Domyślnie WYŁĄCZONE (puste ALLEGRO_CLIENT_ID). Token trzyma dziś odczyt
# zamówień i sondę kształtu (`npm run sonda`), a nowa obsługa klienta
# wystartuje z tego samego parowania. Client_id i secret wydaje rejestracja aplikacji na
# https://developer.allegro.pl (typ „urządzenie” — device flow, bez redirect
# URI). Samo KONTO sprzedawcy paruje się już z panelu /biuro (karta KONTO
# ALLEGRO, rola admin) — token mieszka w bazie aplikacji, nie tutaj, bo każde
# odświeżenie wydaje nową parę.
#export ALLEGRO_CLIENT_ID=
#export ALLEGRO_CLIENT_SECRET=
#
# Nagłówek User-Agent — WYMAGANY, nie opcjonalny. Allegro żąda go w KAŻDYM
# żądaniu i ostrzega, że brak grozi zablokowaniem klucza API; blokada adresu
# IP wygląda potem jak awaria integracji. Po rejestracji aplikacji kliknij
# „Wygeneruj nagłówek User-Agent" i wklej wartość tutaj. Pusta wartość daje
# fallback WERTIS/<wersja>, a STAN SYSTEMU zgłosi to jako problem.
export ALLEGRO_USER_AGENT=
#
# 1 = środowisko testowe (allegro.pl.allegrosandbox.pl). Token nie przeżywa
# zmiany środowiska — po przełączeniu trzeba sparować konto ponownie.
#export ALLEGRO_SANDBOX=0
#
# Wymuszenie trybu: dev = bez kontaktu z Allegro (demo/testy), http = prawdziwe
# API. Puste = wynika z SGT_MODE (seeded→dev, mssql→http).
#export ALLEGRO_MODE=
#
# ── Zwroty klienckie (0.150.0) ──────────────────────────────────────────────
#
# Takt synchronizacji zwrotów w milisekundach; 0 wyłącza ticker. Domyślnie
# pięć minut, czyli PIĘĆ RAZY RZADZIEJ niż skrzynka — i to jest decyzja,
# nie zaniedbanie. Zwrot ma termin liczony w dniach, więc pięć minut
# opóźnienia nie kosztuje nic; pytanie klienta czeka na odpowiedź. Dwa
# tickery bijące z jednego adresu równym rytmem to ta sygnatura maszyny,
# którą anti-bot Allegro rozpoznaje.
#export ALLEGRO_ZWROTY_SYNC_MS=300000
#
# Takt synchronizacji reklamacji (/sale/issues); 0 wyłącza ticker. GĘŚCIEJ niż
# zwroty, bo reklamacja niesie czat: klient pisze i czeka na odpowiedź tak samo
# jak w skrzynce, a termin decyzji bywa krótszy niż zegar zwrotu.
#export ALLEGRO_REKLAMACJE_SYNC_MS=180000
#
# Od kiedy skrzynka widzi rozmowy. Data ISO; puste znaczy „bez granicy".
# Granica działa na WĄTEK, nie na wiadomość: rozmowa z jakąkolwiek wiadomością
# po tej dacie wchodzi w całości, razem z wcześniejszym kontekstem. Agent,
# który widzi pytanie bez jego początku, odpowiada w ciemno.
#
# Domyślnie 1 września 2026, północ czasu lokalnego — czyli 31 sierpnia 22:00
# w UTC, bo tak przychodzą daty z Allegro.
#
# UWAGA: przesunięcie progu W PRZÓD kasuje przy najbliższym starcie rozmowy,
# które znalazły się przed nim. Wracają dopiero po cofnięciu progu i ponownej
# synchronizacji.
#export ALLEGRO_INBOX_OD=2026-08-31T22:00:00Z
#
# Od kiedy widzimy zwroty. Data ISO, PRÓG BEZWZGLĘDNY: obowiązuje zawsze,
# także wtedy, gdy kursor już stoi. Zastąpiło okno względne
# ALLEGRO_ZWROTY_DNI_WSTECZ, które liczyło się wyłącznie przy pierwszym
# przebiegu — zwrot sprzed granicy wjeżdżał potem przy pierwszej zmianie
# po stronie Allegro. Puste znaczy „bez granicy".
#
# Domyślnie 20 SIERPNIA 2026, północ czasu lokalnego. W kodzie i tutaj data
# stoi w UTC, bo tak przychodzą daty z Allegro — stąd 19 sierpnia 22:00.
# Do 0.152.0 stało tu 20 lipca; właściciel przesunął próg o miesiąc.
#export ALLEGRO_ZWROTY_OD=2026-08-19T22:00:00Z
#
# Od kiedy kolejka reklamacji POKAZUJE sprawy. To próg widoku, nie pobierania:
# starsze sprawy dalej się synchronizują, tylko nie stoją na liście. Puste
# znaczy „bez progu".
#export REKLAMACJE_OD=2026-06-30T22:00:00Z
#
# Takt uzupełniania zamówień do zwrotów; 0 wyłącza ticker. Najrzadszy z trzech
# tickerów Allegro, bo dociąga wyłącznie zamówienia do zwrotów, które już
# mamy — po kilku przebiegach nie ma czego pobierać i milczy.
#export ALLEGRO_ZAMOWIENIA_SYNC_MS=600000
#
# ── Odnośniki do panelu sprzedawcy Allegro ──────────────────────────────────
#
# `{id}` w miejscu identyfikatora. To są strony UI, a NIE API, więc nie
# opisuje ich ani specyfikacja w docs/allegro/swagger.yaml, ani nic innego.
#
# Gdy odnośnik trafi w 404, popraw wzorzec TUTAJ; to nie wymaga nowej wersji
# aplikacji. Pusta wartość wyłącza odnośnik i zostawia sam tekst.
#
# ZWROT prowadzi do Centrum Sprzedaży i jako jedyny z tych wzorców przeszedł
# przez żywe konto. `{od}` to dolna granica zakresu dat listy — wstawiamy tam
# dzień zgłoszenia zwrotu, bo stała granica wycięłaby starszy zwrot i lista
# wyszłaby pusta mimo trafionego numeru. Wzorzec dla SANDBOKSU zostaje dawny:
# hosta Centrum Sprzedaży dla sandboksu nie znamy.
#export ALLEGRO_PANEL_ZWROT=https://salescenter.allegro.com/returns?page=1&limit=25&from={od}&search={id}
#
# ZAMÓWIENIE i OFERTA dalej noszą [WERYFIKUJ] — to są założenia.
#export ALLEGRO_PANEL_ZAMOWIENIE=https://allegro.pl/moje-allegro/sprzedaz/zamowienia/{id}
#
# Ile dni ma sprzedawca na OBSŁUŻENIE zwrotu, licząc OD JEGO OTRZYMANIA.
# Siedem — regulamin Allegro. Do 0.338.0 stało tu czternaście liczonych od
# zgłoszenia klienta, czyli termin ustawowy na oddanie pieniędzy; decyzja
# właściciela z 0.339.0: kolejką pracy rządzi ten zegar, po którym Allegro
# rozlicza sprzedawcę. Termin ustawowy obowiązuje dalej z mocy prawa.
#
# UWAGA PRZY AKTUALIZACJI: jeśli ta linia jest u Ciebie ODKOMENTOWANA ze starą
# czternastką, zostaje czternastka — wartość z pliku przykrywa domyślną.
#
# Stoi w konfiguracji, bo to liczba z cudzego regulaminu, a nie z naszego kodu:
# zmiana zasad ma być wpisem tutaj, nie nowym wydaniem aplikacji.
#export ZWROT_TERMIN_DNI=7
#
# Po ilu dniach od ZGŁOSZENIA zwrot bez decyzji wychodzi z kolejki „Do decyzji"
# (0.452.0). Sprzedawca ma siedem dni na oddanie pieniędzy, a ósmego dnia
# Allegro oddaje je samo — po progu wypłata jest faktem. Czterdzieści pięć
# wybrał właściciel, z zapasem na paczki, które długo jechały. Nic nie jest
# zapisywane: zmiana liczby działa od razu i w obie strony.
#export ZWROT_WYGASA_DNI=45
#
# Od kiedy raport DO SPRAWDZENIA woła o zwrot rozliczony poza aplikacją.
# Domyślnie dzień wdrożenia 0.340.0 (15 września 2026, północ czasu
# lokalnego = 14 września 22:00 UTC). Historia sprzed progu milczy: korekt
# do tamtych zwrotów nikt już wstecz nie wystawi, a raport z setką wierszy
# przestaje być czytany. Pusta wartość znaczy „wołaj o wszystkie".
#export ZWROT_ROZLICZONE_OD=2026-09-14T22:00:00Z
#
# Ręczne WPISYWANIE lokalizacji na kolektorze (obok skanowania). Domyślnie
# włączone. `0` chowa tę ścieżkę na trzech ekranach kolektora — dyscyplina
# „tylko skan" dla magazynów, w których każda półka ma etykietę z kodem.
#export ALLOW_MANUAL_LOC=1
#
# Limit pojedynczego zapytania do bazy Subiekta [ms]. Domyślnie 30000 —
# 15 s drivera bywało za mało dla odczytów wsadowych importu (stany to
# ponad sto tysięcy wierszy) na maszynie obciążonej pracującym Subiektem.
#export MSSQL_REQUEST_TIMEOUT_MS=30000

# ── Copilot: rozpoznanie, o co pyta klient (etap F) ─────────────────────────
#
# WYŁĄCZONY DOMYŚLNIE i brak klucza NIE ZATRZYMUJE STARTU. Odwrotność 0.84.x,
# gdzie ustawiony dostawca bez klucza wywracał serwer, a usługa NSSM wpadała
# w pętlę restartów. Przy wyłączonym Copilocie panel mówi wprost, że jest
# niepodłączony, i nie pokazuje przycisku, który i tak nie zadziała.
#export COPILOT_MODE=anthropic
#
# KLUCZ STOI W OSOBNEJ ZMIENNEJ I TYLKO TAM. SDK Anthropica czyta ją sam;
# serwer nie kopiuje jej do konfiguracji, więc nie ma jej w komunikatach
# błędów ani w zrzutach diagnostycznych. W 0.84.1 klucz wklejony do pola
# TRYBU wylądował w logs\wertis-api.err.log i trzeba go było unieważnić —
# pola trybu i modelu poniżej maskują swoją wartość w komunikacie, ale
# najtańsza obrona to wkleić klucz tutaj.
#export ANTHROPIC_API_KEY=sk-ant-...
#
# Model. Zejście na tańszy ma być JEDNĄ zmianą w tym pliku, podjętą po
# pomiarze trafności z ekranu ustawień — dlatego nie stoi w kodzie.
#export COPILOT_MODEL=claude-opus-5
#
# Model WYŁĄCZNIE do klasyfikacji wiadomości (22 września 2026). Puste pole
# znaczy „jak COPILOT_MODEL". Osobno, bo zejście na tańszy model przy etykiecie
# nie ma ciągnąć w dół szkicu, który czyta klient. Zmieniaj po tygodniu
# etykiet na karcie „Copilot" — precyzja kategorii ma się utrzymać w przedziale.
#export COPILOT_MODEL_KLASYFIKACJA=claude-sonnet-5
#
# Ile rozmów bierze jedno kliknięcie. To jest hamulec na WYDATEK, nie
# szczegół wyglądu przycisku: partia większa niż ten limit dostaje odmowę
# ze zdaniem nazywającym limit.
#export COPILOT_MAX_PARTIA=20
#
# ── Szkic SAM dla nowego pytania pod ofertą (0.317.0) ──────────────────────
# WYŁĄCZONE DOMYŚLNIE i to nie jest ostrożność na zapas. Przełącznik
# automatycznych szkiców istniał już raz, w 0.107.0, i stał na zerze na
# wyraźną prośbę właściciela. Wraca na jego prośbę, ale wraca wyłączony:
# rzecz, która wydaje pieniądze BEZ KLIKNIĘCIA, ma się włączać decyzją
# podjętą tutaj, a nie skutkiem ubocznym aktualizacji.
#
# Bierze WYŁĄCZNIE wiadomości przychodzące niosące numer oferty, czyli
# pytania sprzed zakupu. Do klienta dalej nic nie idzie bez człowieka.
#export COPILOT_AUTO_SZKIC=1
#
# DWA HAMULCE, bo nikt już nie klika. Pierwszy: ile szkiców na jeden
# przebieg — reszta czeka do następnego. Drugi: twardy sufit na godzinę,
# liczony z księgi wywołań RAZEM z tymi zakończonymi błędem, bo nieudane
# wywołanie też kosztuje. Bez sufitu fala pytań płaci się sama, a rachunek
# przychodzi po fakcie.
#export COPILOT_AUTO_NA_PRZEBIEG=5
#export COPILOT_AUTO_NA_GODZINE=30
#
# ── Rozpoznanie KAŻDEJ nowej wiadomości klienta (22 września 2026) ──────────
# WYŁĄCZONE DOMYŚLNIE z tego samego powodu co szkic z taktu: wydaje pieniądze
# bez kliknięcia. Włączone rozpoznaje ostatnią wiadomość klienta w każdej
# rozmowie z okna, a przycisk nad kolejką zostaje. Nic nie idzie do klienta.
#export COPILOT_AUTO_KLASYFIKACJA=1
#
# Szkic zaraz po rozpoznaniu, w tym samym takcie. Włączony domyślnie, gdy
# rozpoznanie jest włączone; 0 zostawia samo rozpoznanie.
#export COPILOT_SZKIC_PO_ROZPOZNANIU=0
#
# Hamulec na przebieg i sufit na godzinę — sufit liczony z księgi wywołań
# razem z błędami.
#export COPILOT_AUTO_KLASYFIKACJA_NA_PRZEBIEG=10
#export COPILOT_AUTO_KLASYFIKACJA_NA_GODZINE=60
#
# Ile dni wstecz takt sięga. Bez okna pierwsze włączenie przerobiłoby całą
# historię skrzynki na koszt firmy.
#export COPILOT_KLASYFIKACJA_OKNO_DNI=7
#
# Struktura wątku (typ, podtyp, zamówienia) z beta.v1 przy synchronizacji
# skrzynki: jedno dodatkowe żądanie na wątek, w którym coś się zmieniło.
# Włączone domyślnie; odmowa Allegro wstrzymuje je samo na sześć godzin.
# Nic nie kosztuje po stronie dostawcy modelu — to odczyt z Allegro.
#export ALLEGRO_WATKI_BETA=0

# ── Strefa czasowa do WYŚWIETLANIA godzin ───────────────────────────────────
#
# W bazie wszystko zostaje w UTC — to jest wyłącznie warstwa prezentacji
# (godziny w kolejce zapisów). Ustawienie, nie strefa maszyny: serwer bywa
# postawiony z angielskiego obrazu systemu, a wtedy godzina na kolektorze
# rozjeżdża się z zegarem na ścianie bez żadnego tropu.
#
# Od 0.487.0 wyznacza też okno nocnej kopii bazy (1:00–5:00 na zegarze
# magazynu), więc zła strefa przesuwa kopię w godziny pracy.
#export STREFA_CZASU=Europe/Warsaw

# ── Kopie bazy aplikacji (0.487.0) ──────────────────────────────────────────
#
# Serwer robi je SAM: przed każdą migracją schematu i co noc. Nie ma czego
# włączać. Domyślnie lądują w server\data\kopie, czyli na tym samym dysku co
# baza — chronią przed migracją i pomyłką, nie przed padnięciem dysku.
# Ten klucz przenosi je na inny dysk. Opis i przywracanie: DEPLOY.md §7.
#export KOPIE_KATALOG=D:\kopie-wertis

# ── Aktualizacja automatyczna (0.494.0) ────────────────────────────────────
#
# Serwer sam wgrywa nowe wydanie przez to samo zadanie Harmonogramu, które
# uruchamia przycisk w panelu (DEPLOY.md §0b). Wydanie z pozycją
# „[wymaga działania]" NIE wchodzi samo — czeka na przycisk. Nieudane wraca
# samo do poprzedniej wersji i drugi raz automatycznie nie wchodzi.
#
# noc — w oknie AKTUALIZACJA_OKNO, gdy serwer od dziesięciu minut stoi
#       bezczynnie (domyślne na produkcji); zaraz — od razu (domyślne na dev);
#       wylaczona — tylko przycisk.
#export AKTUALIZACJA_AUTO=noc
#
# Godziny okna na zegarze magazynu. Domyślne 3-5 idzie po nocnej kopii bazy.
#export AKTUALIZACJA_OKNO=3-5
#
# Wydanie musi mieć tyle godzin, zanim wejdzie samo — czas na to, żeby dev
# albo ktoś w biurze zauważył błąd. Produkcja 6, dev 0.
#export AKTUALIZACJA_DOJRZALOSC_H=6
#
# Adres instancji dev na tej samej maszynie. Gdy ustawiony, produkcja wgrywa
# wyłącznie wersję, na której dev już pracuje i odpowiada zdrowo.
#export AKTUALIZACJA_KANAREK=http://localhost:3002

# ── Druga instancja na tej samej maszynie (dev obok produkcji) ──────────────
#
# Instalator stawia ją przełącznikiem -Dev (DEPLOY.md, rozdział o środowisku
# dev). Przy stawianiu RĘCZNYM trzy klucze rozstrzygają o izolacji:
#
# PORT — bez niego druga instancja wejdzie na 3001 i przegra z produkcją.
#export PORT=3002
#
# DB_PATH — domyślna ścieżka liczy się względem katalogu instalacji, więc dwa
# OSOBNE katalogi mają osobne bazy same z siebie. Ten klucz jest potrzebny
# wyłącznie przy dwóch procesach z JEDNEGO katalogu — wtedy bez niego oba
# piszą do tej samej bazy, a to psuje się bez jednego komunikatu.
#export DB_PATH=C:\wertis-dev\server\data\wertis.db
#
# SRODOWISKO — etykieta instancji, nie tryb. /api/health ją zwraca, a biuro
# i kolektor rysują z niej czerwone ostrzeżenie. Produkcja NIE ustawia nic.
#export SRODOWISKO=dev

# ── Reszta ma sensowne domyślne ─────────────────────────────────────────────

# ── Kolejka wiedzy opróżnia się sama (0.331.0) ─────────────────────────────
# ODWRÓCENIE ZASADY, nie jej rozszerzenie: do 0.330.0 wiedzę zatwierdzał
# wyłącznie człowiek z biura. Właściciel, pytany wprost i ze wskazaniem
# ryzyka, wybrał opróżnianie kolejki automatem. Wpisy automatu karmią czwarty
# szczebel doboru, więc zła marka wraca do klienta jako zła część.
# Domyślnie wyłączone. Lista do prostowania stoi w panelu, w Ustawieniach.
# WIEDZA_AUTOMAT=1
#
# Ile wierszy na jeden przebieg — osobno dla kluczy, zastosowań i pasowań.
# Nie podnoś przed pierwszym uruchomieniem na zaległej kolejce.
# WIEDZA_AUTOMAT_NA_PRZEBIEG=25
#
# Czy wolno dopytać model językowy, gdy trzy źródła deterministyczne milczą.
# Osobno od wyłącznika głównego: to jedyna część automatu, która kosztuje,
# i jedyna, która nie stoi na naszych danych. Wymaga COPILOT_MODE=anthropic
# i klucza.
# WIEDZA_AUTOMAT_MODEL=1
